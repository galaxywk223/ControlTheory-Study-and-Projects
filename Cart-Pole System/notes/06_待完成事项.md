# 待完成事项

## 1. 高优先级

### 1.1 建立统一对照表

需要建立一张统一对照表，覆盖三条路线的：

- 控制方法；
- 使用模型；
- 输出结果；
- 评价指标；
- 当前结论。

### 1.2 建立统一评价指标

建议至少补充以下指标：

- 稳定时间；
- 最大摆角；
- 小车位置峰值；
- 控制输入能量；
- 强化学习回合长度或成功率；
- 系统辨识拟合优度。

## 2. 中优先级

### 2.1 统一图表说明

当前虽然已经将图片归档，但还缺少：

- 图号；
- 结果来源脚本；
- 使用参数说明；
- 与其他路线的可比性说明。

### 2.2 统一脚本输出

当前脚本输出已经开始分流到 `assets/`、`data/` 和 `runs/`，但仍需要进一步统一：

- 训练结果目录命名；
- 图片文件命名；
- 数据文件命名；
- 输出日志位置。

## 3. 低优先级

### 3.1 扩展到非线性模型

当前三条路线大多仍围绕线性化倒立摆模型展开。若继续推进，可以考虑：

- 非线性模型仿真；
- 扰动与参数不确定性；
- 更真实的控制约束。

### 3.2 增加正式实验记录

后续可以补充：

- 实验参数表；
- 多组结果对照；
- 更正式的结论总结。
